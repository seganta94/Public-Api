<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Current Stock Information</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body class="bg-dark text-white">
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div>
            <h1 class="text-center mb-5">Public API - Current Stock Information</h1>

            <form id="myForm" method="post" action="/stock" class="text-center">
                <div class="form-group">
                    <label for="stockTicker"><b>Stock Ticker:</b></label>
                    <input type="text" class="form-control" id="stockTicker" name="stockTicker">
                </div>
                <button type="submit" class="btn btn-primary mb-5">Submit</button>
            </form>

            <div class="response-area">
                <% if (locals.content) { %>
                    <ul class="list-unstyled text-center">
                        <li><b>Symbol:</b> <%= content.symbol %></li>
                        <li><b>Date:</b> <%= new Date(content.date).toLocaleDateString() %></li>
                        <li><b>Volume:</b> <%= content.volume.toLocaleString() %></li>
                        <li><b>Open:</b> $<%= content.open.toFixed(2) %></li>
                        <li><b>Close:</b> $<%= content.close.toFixed(2) %></li>
                        <li><b>High:</b> $<%= content.high.toFixed(2) %></li>
                        <li><b>Low:</b> $<%= content.low.toFixed(2) %></li>
                    </ul>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
